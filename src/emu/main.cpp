// Dependencies
#include <emscripten.h>

// Modules
#include "../core/cpu.h"
#include "../core/graphics.h"


int main() {
    CPU cpu;
    cpu.reset();

    // Initialize graphics
    render(0, NULL);

    // Load program into memory

    // Execute program
    
    
    return 0;
}